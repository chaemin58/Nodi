# 📍 Nodi — 프로젝트 요약 (v1 기획서)

> 이 문서 하나로 새 세션에서 맥락을 이어갈 수 있습니다.
> 새 Claude Code 세션을 이 폴더(`C:\codeit\nodi`)에서 열고, "plan.md 읽고 이어서 하자"라고 시작하세요.

## 한 줄 정의

친구들과 **약속 장소를 함께 정하는** 웹 서비스. "여기 어때?" 하고 흩어지던 장소들을 한 보드에 모아 투표로 정하고, 카톡으로 공유.

## 문제 & 해결

- **문제:** 네이버 지도에서 장소 찾기 → 링크 복사 → 카톡 전송, 근데 카톡은 대화가 섞여서 "우리 어디 가기로 했지?"를 못 찾음
- **해결:** 약속마다 보드 하나. 장소를 검색해 바로 담고, 투표로 정하고, 확정된 곳이 안 흩어짐

## 포지셔닝 (중요)

- **카톡을 대체하지 않는다** — 정면승부는 짐. 대화는 카톡에 두고, Nodi는 "장소 찾고 정리·결정"만 담당
- **앱 내 채팅 없음** — 채팅을 넣으면 "카톡의 열등한 복제품"이 됨. 대화 필요하면 카톡으로 넘김. Nodi는 장소·결정만
- **협업(같이 정하기)이 무기** — 단순 북마크는 네이버 지도가 압도적이라 안 됨. 네이버에 없는 "여럿이 함께 결정"이 차별점
- **카톡 공유 = 바이럴 엔진**, 비회원도 링크로 보고 투표
- **공유와 결정은 한 흐름** — "장소 모아 공유(앞) → 그중 정하기(뒤)". 공유만 있으면 얇고, 투표만 있으면 저빈도. 둘을 한 흐름으로

## 브랜드

- **이름:** Nodi (노디) = node + 어디
- **로고:** 리플(중심점에서 퍼지는 동심원) — 지도 핀은 안 씀(데이홉과 중복 회피)
- **컬러:** 딥그린 `#1D9E75`
- **폰트:** Pretendard

## 로드맵

| 단계   | 기능                                                                   | 역할                   |
| ------ | ---------------------------------------------------------------------- | ---------------------- |
| **v1** | 모임 + 약속 보드 (장소 모으기·투표·확정·카톡 공유) + 모임 stats 스트립 | 코어 + 바이럴          |
| **v2** | ⭐ 커플 타입 (D-day·데이트 지도)                                       | 고빈도 리텐션 세그먼트 |
| **v2** | ⭐ 장소 팔로우 + 활동 피드                                             | 시그니처 차별점        |
| **v2** | 중간지점 찾기 (공평한 약속 장소 추천)                                  | 약속 안 기능           |
| **v3** | 사람 팔로우 → 소셜 완성                                                | 소셜 리텐션            |

**메모:**

- 중간지점은 저빈도라 정체성이 아닌 "기능"으로.
- 커플 타입은 데이터 구조가 친구 모임과 동일 → v1에 `groups.type` 컬럼만 심어두면 v2에서 화면만 추가하면 됨(마이그레이션 없음).
- 모임 stats(함께한 지 N일·N번째·마지막 만남)는 기존 데이터로 계산되므로 v1에 가볍게 넣어도 됨.
- 장소 팔로우 콘텐츠는 "가게가 올리는 소식"(❌ 입점 지옥)이 아니라 "내 네트워크의 그 장소 활동"(✅ 내 데이터)으로.

## 구조: 모임별 계층

```
모임(groups)          ← 지속되는 사람 그룹 (카톡방 개념). 멤버가 여기 소속
 └─ 약속(meetups)      ← 그 모임 안의 "어디 갈까" 한 번의 결정 라운드
     └─ 후보장소(places)
         └─ 투표(votes)
```

- 이유: 약속별(일회용)은 리텐션 약함. 모임별은 재사용·기록 누적으로 재방문 유도
- ※ 앞서 그린 목업의 "보드 상세"는 이제 **약속(meetup) 상세**에 해당. 위에 모임 목록/모임 상세 단계가 추가됨
- **모임 타입:** `groups.type` = `friends`(v1) · `couple`(v2) · `family`. 애인=2명짜리 모임. 구조 동일, 화면/톤만 분기(커플은 투표 대신 ♥반응, D-day, 데이트 지도)

## 설계한 화면 (목업 완료 — 일부는 새 구조로 재배치 필요)

1. 홈 (모임 목록)
2. 모임 상세 (약속 라운드 목록) ← 신규 단계
3. 약속 상세 (후보 장소·투표·확정·연결된 채팅) ← 기존 "보드 상세"
4. 장소 검색/추가 (검색 + 지도)
5. 중간지점 찾기 (참여자 위치 → 공평 중심점 + 근처 추천)

**남은 v1 화면:** 모임 만들기·초대 · 약속 만들기 · 카톡 공유+비회원 링크 뷰 · 확정 상태 · 로그인/온보딩 · 랜딩

## 기술 스택 (백엔드 직접 안 짬)

- **프론트:** Next.js (+ Vercel 배포)
- **백엔드 대체:** Supabase (Postgres + 인증 + 실시간)
- **지도:** 네이버 지도 (Naver Maps JS API, 네이버 클라우드 플랫폼)
- **장소 검색:** 네이버 검색 API (지역 검색)
- **로그인:** Supabase Auth + 카카오 OAuth
  - 카카오 로그인은 Supabase에 기본 내장이라 초보자도 설정만으로 됨
  - 네이버 로그인은 내장이 아니라 직접 연결해야 해서 나중에(선택)
- ※ 나중에 커스텀 로직 필요하면 Supabase Edge Functions / Next.js 서버로 점진 확장

### 지도 관련 참고

- 네이버 지역 검색 API는 결과 수 제한(최대 5개)과 상업적 사용 조건이 있음.
  검색 품질이 아쉬우면 나중에 카카오 로컬 API를 검색용으로 병행하는 것도 방법.

## 데이터 설계 (완료 — `supabase/schema.sql`)

- **테이블 6개:** `profiles` · `groups`(모임, `type` 포함) · `group_members` · `meetups`(약속) · `places` · `votes`
- **RLS(권한):** 테이블별 읽기/추가/수정·삭제 규칙 확정. `security definer` 도우미로 무한루프 회피, `is_shared`로 비회원 공유 처리(약속 단위), `unique` 제약으로 중복투표 차단
- **실시간:** `meetups`·`places`·`votes` 라이브 업데이트 켜둠
- `groups.type`은 v1에 심어두되 전부 `'friends'`로만 사용 (커플 v2 대비)
- **확정 = 코스(여러 장소):** 한 곳만 정하는 게 아니라 밥·카페·술을 순서대로 묶음.
  `places.is_confirmed`(코스 포함 여부) + `places.course_order`(순서)로 표현. 단일 `confirmed_place_id`는 제거(→ 순환참조 FK도 사라져 단순해짐).
- **장소 카테고리:** `places.category`(밥/카페/술/놀거리 등) — 구분 없이 담고 태그로만 표시

## UI 규칙

### 사이드바

```
[+ 새 모임]        ← 강조 버튼
내 모임
알림              ← 안읽음 뱃지
────
저장한 nodi
다녀온 nodi
────
(내 프로필)        ← 하단 고정
```

- PC: 알림은 팝오버 / 모바일: 알림은 전체 화면으로 이동
- 채팅 메뉴 없음(포지셔닝 참고). 탐색/피드는 v3

### 상태 뱃지 (약속에 하나만 — 상호배타)

- `정하는 중`(투표중) · `정해짐`(확정) · `다녀옴`(지남)

### 넛지 뱃지 (조건 맞을 때 겹쳐 뜸)

- `슬슬 만날 때`(오래 안 만남) · `D-3`(약속 임박) · `새 모임`
- 톤 주의: "만난 지 오래됐어요"(지적) ❌ → "슬슬 만날 때"(부드럽게) ✅

### 뱃지 색상 (배경 / 글자)

| 뱃지                    | 배경      | 글자      |
| ----------------------- | --------- | --------- |
| 정하는 중 (앰버)        | `#FAEEDA` | `#854F0B` |
| 정해짐 / 새 모임 (초록) | `#E1F5EE` | `#0F6E56` |
| 다녀옴 (회색)           | `#F1EFE8` | `#5F5E5A` |
| 슬슬 만날 때 (코럴)     | `#FAECE7` | `#993C1D` |
| D-3 (블루)              | `#E6F1FB` | `#185FA5` |
| 커플 포인트 (핑크)      | `#FBEAF0` | `#993556` |

### 브랜드 색

- 메인 딥그린 `#1D9E75` · 진한 그린(글자/아이콘) `#0F6E56` · 연한 그린(틴트) `#E1F5EE`

### 모임 카드 구성 (안 휑하게)

- 상단 블록 = 정체성: **멤버 아바타 + 최근 활동/다녀온 곳**
- 하단 = 지금 상황: **제목 + 상태·넛지 뱃지 + 이번 약속 요약 한 줄(예: "주말 브런치 · 후보 4곳") + 메타(멤버 수·N번째·마지막 만남)**
- 최소 조합 = [멤버 아바타 + 이번 약속 요약 + 상태 뱃지]. 카운트("38번 nodi")만 있으면 정적이라 휑함

### 약속 상세 — 투표·확정 규칙

- **후보 장소:** 카테고리 구분 없이 다 담음(태그로만 밥/카페/술 표시). 각 카드에 투표수 + **내 위치 기준 거리**(v1은 직선거리, v2에서 이동시간)
- **확정 = 코스:** 한 곳이 아니라 여러 곳을 다중 선택 → 순서 있는 코스로 확정. 오른쪽 "이번 코스" 패널에 1→2→3 + 이동시간
- **표 1위 ≠ 확정:** 표는 참고용. 원하는 곳(들)을 골라 확정 가능. 1위 아닌 걸 고르면 "표 1위는 ○○예요" 넛지로 확인
- **오른쪽 패널:** 채팅 대신 → 카톡 공유 + 투표 현황(참여율·안 한 사람) + 이번 코스

---

## ✅ 내가 직접 해야 하는 것 (계정·키 — Claude가 대신 못 함)

### 1. Supabase

- [ ] supabase.com 가입 → New project 생성
- [ ] SQL Editor에 `supabase/schema.sql` 붙여넣고 Run
- [ ] Settings → API 에서 **Project URL** + **anon key** 복사해두기

### 2. 네이버 클라우드 플랫폼 (지도)

- [ ] console.ncloud.com 가입 → Maps 서비스 신청
- [ ] **Client ID** 발급 (지도 표시용)
- [ ] 웹 서비스 URL 등록 (localhost + 배포 도메인)

### 3. 네이버 개발자 (장소 검색)

- [ ] developers.naver.com → 애플리케이션 등록 → 검색 API 사용
- [ ] **Client ID / Client Secret** 발급

### 4. 카카오 개발자 (로그인)

- [ ] developers.kakao.com 가입 → 앱 생성
- [ ] 카카오 로그인 활성화 + Redirect URI 등록 (Supabase가 알려주는 주소)
- [ ] **REST API 키** 복사

### 5. (나중) Vercel — 배포할 때

## 🔵 Claude(새 세션)가 하는 것

- Next.js 프로젝트 뼈대 세팅
- Supabase / 네이버 지도 / 네이버 검색 연결 코드
- 위에서 받은 키를 `.env`에 넣기 (값은 내가 붙여줌)
- 홈 화면부터 실제 화면 코딩

## 다음 순서

1. Supabase 가입 + 프로젝트 + SQL 실행 + 키 복사
2. 네이버(지도+검색) + 카카오(로그인) 키 발급
3. 이 폴더에서 새 Claude Code 세션 시작
4. "plan.md 읽고 Next.js 세팅부터 시작하자"라고 말하기
5. 코드 작성 (Claude)
